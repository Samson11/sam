const initState = {
  projects: [
    {id: 1, title: 'Dummy Project', content: 'My First Dummy Project'}
  ]
}
const projectReducer = (state = initState, action) => {
  return state
}

export default projectReducer;
